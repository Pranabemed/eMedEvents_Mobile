/**
 * Test debug module. Contains application logic, configuration, or shared helpers.
 */

// Quick script to see what `store.getActions()` is.
// Actually we can just run jest with --verbose to trace.
